# v89 — Final core-backend compatibility

Full core-backend package for Search Worker v76 and Frontend v158. Permanent NGO-ID propagation and historical backfill remain enabled. Karnataka Recovery ownership evidence is preserved in downstream records. Protected mutations use only the existing `ADMIN_PASSWORD`.
